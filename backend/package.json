{
  "name": "scrubprep-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Scrub Prep Back4App Cloud Code — server-side OpenAI integration for OR Prep, Pimp Me, and Rapid Fire.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "build:vendor": "node scripts/build-app-store-vendor-bundle.js"
  },
  "dependencies": {
    "@apple/app-store-server-library": "3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
